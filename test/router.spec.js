const expect = require('chai').expect;
const Router = require('../lib/client/modules/Router');

describe('router-suite', () => {
  const router = new Router();

  it('should asynchronously load a route properly', () => {
    expect(true).to.be.true;
  });

  it('should maintain proper router state', () => {
    expect(true).to.be.true;
  });

});
